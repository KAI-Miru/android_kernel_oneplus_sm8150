name: Miru H.40 properfix2 builder superseded

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  preserved:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: |
          echo "This workflow is preserved for diagnostics only."
          echo "The final workflow pins external modules after the compatibility injector."
