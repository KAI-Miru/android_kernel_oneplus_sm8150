name: Miru LTS 4.14.190 merge scaffold (completed)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  completed:
    runs-on: ubuntu-latest
    steps:
      - name: Report completion
        run: |
          echo "The guarded Android 4.14.190 merge scaffold was created successfully."
          echo "Merge commit: 5d8cba39fefb935c6feaf30ea1a57dfffa80273a"
          echo "Further integration must resolve the documented deferred conflicts manually."
