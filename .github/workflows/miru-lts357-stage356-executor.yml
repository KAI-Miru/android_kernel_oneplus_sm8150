name: Miru H.40 OpenELA stage 356 guarded merge and checkpoint

on:
  pull_request:
    branches: [miru-h40]
    types: [opened, reopened, synchronize]
    paths:
      - '.github/workflows/miru-lts357-stage356-executor.yml'

permissions:
  contents: read

concurrency:
  group: miru-h40-lts357-stage356-executor
  cancel-in-progress: true

jobs:
  maintenance:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Stage 356 executor maintenance commit; no source action.'
