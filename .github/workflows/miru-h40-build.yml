name: Miru H.40 Linux 4.14.336 validated release workflow

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  preserved:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Full release validation is preserved at commit 4e66a4c01e00b5ac6def9ca5f02be8f8683c621b; automatic runs are disabled during physical regression isolation."
