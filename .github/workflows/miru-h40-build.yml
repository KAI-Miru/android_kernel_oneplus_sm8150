name: Miru H.40 inherited diagnostic disabled on diag29

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled-marker:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Inherited 4.14.291 workflow intentionally disabled on diag29 branch"
