name: Miru H.40 properfix1 builder preserved

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  preserved:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: |
          echo "The previous properfix1 production builder is preserved in repository history."
          echo "Use the dedicated properfix2 USB/audio workflow for the current candidate."
