name: Miru H.40 Linux 4.14.336 builder preserved

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  preserved:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: |
          echo "The reviewed properfix1 production builder is preserved at commit c8f4c778d5bbc3032e825b74f93f2ba12e2644b7."
          echo "Automatic and manual builds are intentionally disabled while the new USB source fix remains unbuilt and physically untested."
