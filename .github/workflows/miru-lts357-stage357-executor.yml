name: Miru H.40 OpenELA stage 357 guarded merge and checkpoint

on:
  pull_request:
    branches: [miru-h40]
    types: [opened, reopened, synchronize]
    paths:
      - '.github/workflows/miru-lts357-stage357-executor.yml'

permissions:
  contents: read

concurrency:
  group: miru-h40-lts357-stage357-executor
  cancel-in-progress: true

jobs:
  maintenance:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Stage 357 executor maintenance commit; no source action.'
